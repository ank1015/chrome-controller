# TODO

- Add end-to-end live coverage for `element` commands against real pages, including links that open a new tab or window, so we catch user-gesture and duplicate-activation regressions.
- Add end-to-end live coverage for `page find` and top-level `find`, including token/auth preflight or clearer skip behavior when the LLM auth token is expired.
